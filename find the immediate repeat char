//i love apple
//p

#include <stdio.h>
#include<string.h>
int main()
{
char s[30];
gets(s);
int a=30;
for(int i=0;s[i]!='\0';i++)
{
    for(int j=i+1;s[j]!='\0';j++)
    {
       if(s[i]==s[j]) //here just we need to find the sudden repeating character(lowest index)
       {
        if(j<a)//if j is less than a then it valued stored to j if not leave it
           a=j;
       }
    }
}
printf("%c",s[a]);
}
